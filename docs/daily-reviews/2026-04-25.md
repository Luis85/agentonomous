---
date: 2026-04-25
range: 0390614f657b4ddbbabcee057f4392ac71d332b7..682b55727df39ca9a25a9d2c3a243fe393a40d90
commits: 94
blockers: 1
majors: 0
minors: 1
nits: 0
---

## 2026-04-25 — 682b55727df39ca9a25a9d2c3a243fe393a40d90

Reviewed: `0390614f..682b557` (94 commits, 90 files, ~27 K insertions)

Blockers: 1 | Majors: 0 | Minors: 1 | Nits: 0

---

### [BLOCKER] src/ports/LlmProviderPort.ts:22 (and 9 other files)

**Problem:** 16 data-shape `interface` declarations across `src/` where the repo invariant requires `type`.

**Why it matters:** `interface` enables TypeScript declaration merging — a downstream consumer can add fields to `LlmMessage` or `TfjsLearnerOptions` in their own `.d.ts`, silently widening the contract. Library code that assumes the type is closed may accept shapes it cannot handle, producing late-binding surprises that only surface at call sites far removed from the augmenting declaration.

Affected locations (all in `src/`, none are extended or implemented in this diff):

| File | Interfaces |
|---|---|
| `src/ports/LlmProviderPort.ts:22,29,38,49,68,78` | `LlmCacheHint`, `LlmMessage`, `LlmBudget`, `LlmCompleteOptions`, `LlmUsage`, `LlmCompletion` |
| `src/ports/MockLlmProvider.ts:13,25` | `MockLlmScript`, `MockLlmProviderOptions` |
| `src/agent/internal/agentDepsResolver.ts:38,54,82,105` | `ResolvedPorts`, `ResolvedSubsystems`, `ResolvedCognition`, `ResolvedPersistence` |
| `src/agent/internal/buildAgentDeps.ts:41` | `ResolvedDeps` |
| `src/cognition/adapters/tfjs/TfjsLearner.ts:8,15` | `TrainableReasoner<In,Out>`, `TfjsLearnerOptions<In,Out>` |
| `src/agent/createAgent.ts:50` | `CreateAgentConfig` |
| `src/persistence/LocalStorageSnapshotStore.ts:23` | `LocalStorageSnapshotStoreOptions` |

**Fix** (apply pattern to all 16):

```diff
-export interface LlmCacheHint {
-  readonly key: string;
-}
+export type LlmCacheHint = {
+  readonly key: string;
+};
```

For `TrainableReasoner` and `LlmProviderPort` — which represent structural contracts that objects satisfy — `type` is still correct in TypeScript; `implements SomeType` works identically with `type` aliases:

```diff
-export interface LlmProviderPort {
-  complete(...): Promise<LlmCompletion>;
-}
+export type LlmProviderPort = {
+  complete(...): Promise<LlmCompletion>;
+};
```

To prevent recurrence, add to `eslint.config.js`:

```diff
+'@typescript-eslint/consistent-type-definitions': ['error', 'type'],
```

**Counter-argument to my own [BLOCKER]:** TypeScript's structural type system makes `interface` and `type` semantically identical in every non-merging, non-extending context; the runtime impact is precisely zero. The project's ESLint config deliberately omits `consistent-type-definitions`, so these violations compile and pass CI legitimately. The invariant text uses "prefer" rather than "require", which allows judgment. Given that 16 occurrences slipped through across many merged PRs without incident, a pragmatic reviewer might accept them as-is while adding the ESLint rule to prevent future drift.

---

### [MINOR] src/ports/MockLlmProvider.ts (pickScript, ~line 155)

**Problem:** `match-or-error` dispatch mode silently accepts scripts with no `match` predicate at construction time, but those scripts can never fire.

**Why it matters:** A test author writing `dispatch: 'match-or-error'` and accidentally omitting `match` on one entry gets a cryptic `MockLlmProvider: no script matched the request.` at runtime rather than an immediate construction-time `TypeError`. Debugging a mis-configured mock during a failing integration test is significantly harder than a thrown constructor error.

The predicate check in `pickScript`:

```ts
// src/ports/MockLlmProvider.ts ~line 155
const hits = this.scripts.filter((s) => s.match?.(messages, options) === true);
```

`s.match?.()` returns `undefined` when `match` is absent; `undefined === true` is `false`, so the script is invisible to `match-or-error` dispatch.

**Fix** — validate eagerly in the constructor:

```diff
+if ((opts.dispatch ?? 'queue') === 'match-or-error') {
+  const bad = opts.scripts.findIndex((s) => s.match === undefined);
+  if (bad !== -1) {
+    throw new Error(
+      `MockLlmProvider: script[${bad}] has no 'match' predicate ` +
+      `(required in match-or-error mode — positional fallback is disabled).`,
+    );
+  }
+}
```

---

### Summary

- Reviewed range: `0390614f..682b557` (94 commits, 90 files)
- Blockers: 1
- Majors: 0
- Minors: 1
- Nits: 0
- Counter-argument check: BLOCKER (`interface`/`type`) — kept; invariant is explicit even if "prefer" leaves room for judgment. Counter (TypeScript equivalence + no ESLint enforcement) is strong but does not override a stated hard rule.
- Not reviewed: `graphify-out/` (generated graph output), `package-lock.json` (lockfile), `examples/nurture-pet/src/cognitionSwitcher.ts` (189-line demo-only change), `.claude/` config files, `docs/plans/2026-04-24-codebase-review-findings.md` (356-line narrative doc).
- Last reviewed SHA: `682b55727df39ca9a25a9d2c3a243fe393a40d90`
